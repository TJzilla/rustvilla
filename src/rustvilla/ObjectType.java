package rustvilla;

public enum ObjectType
{
	// create enumerators for the different types of objects. Ghost objects will refer to any object which is invisible or has no visible asset associate with it
	CHARACTER, TERRAIN, COLLIDABLE, GHOST, SCRIPT
}
