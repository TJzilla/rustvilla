/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'TJ' at '7/15/16 11:53 AM' with Gradle 2.12
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/2.12/userguide/tutorial_java_projects.html
 */

// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'application'

compileJava {
  sourceCompatibility = '1.8'
  targetCompatibility = '1.8'
}

configurations {
  win64
  win32
  osx
  linux64
  linux32
}

ext.configFile = file "build.properties"
configFile.withReader {
    // Load config.  It shall from now be referenced as simply config or project.config
    def prop = new Properties()
    prop.load(it)
    project.ext.config = new ConfigSlurper().parse prop
}

mainClassName = 'rustvilla.Main'

sourceSets {
    main {
        java {
            srcDir 'src'
        }
        resources {
        	srcDir 'assets'
        }
    }
}


task distWin64(type: Jar) {
	destinationDir = file("$rootDir/build/win/64")
    baseName = project.name + "-" + config.version
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    from { configurations.win64.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
    manifest {
        attributes 'Main-Class': project.mainClassName
    }
}


task distWin32(type: Jar) {
	destinationDir = file("$rootDir/build/win/32")
    baseName = project.name + "-" + config.version
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    from { configurations.win32.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
    manifest {
        attributes 'Main-Class': project.mainClassName
    }
}

task distMac(type: Jar) {
	destinationDir = file("$rootDir/build/osx/universal")
    baseName = project.name + "-" + config.version
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    from { configurations.osx.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
    manifest {
        attributes 'Main-Class': project.mainClassName
    }
}

task distLinux64(type: Jar) {
	destinationDir = file("$rootDir/build/linux/64")
    baseName = project.name + "-" + config.version
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    from { configurations.linux64.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
    manifest {
        attributes 'Main-Class': project.mainClassName
    }
}

task distLinux32(type: Jar) {
	destinationDir = file("$rootDir/build/linux/32")
    baseName = project.name + "-" + config.version
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    from { configurations.linux32.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
    manifest {
        attributes 'Main-Class': project.mainClassName
    }
}

task distAll {
	dependsOn 'distWin64', 'distWin32', 'distMac', 'distLinux64', 'distLinux32'
}
	

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'jcenter' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

// In this section you declare the dependencies for your production and test code
dependencies {
    compile "org.jogamp.jogl:jogl-all:$config.jogAmpVer"
    compile "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer"

    compile "org.jogamp.jogl:jogl-all:$config.jogAmpVer"
    compile "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer"

    win64 "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-windows-amd64"
    win64 "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-windows-amd64"

    win32 "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-windows-i586"
    win32 "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-windows-i586"

    osx "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-macosx-universal"
    osx "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-macosx-universal"

    linux64 "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-linux-amd64"
    linux64 "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-linux-amd64"

    linux32 "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-linux-i586"
    linux32 "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-linux-i586"


    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-android-aarch64"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-android-armv6"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-linux-amd64"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-linux-armv6"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-linux-armv6hf"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-linux-i586"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-macosx-universal"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-solaris-amd64"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-solaris-i586"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-windows-amd64"
    runtime "org.jogamp.gluegen:gluegen-rt:$config.jogAmpVer:natives-windows-i586"

    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-android-aarch64"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-android-armv6"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-linux-amd64"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-linux-armv6"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-linux-armv6hf"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-linux-i586"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-macosx-universal"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-solaris-amd64"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-solaris-i586"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-windows-amd64"
    runtime "org.jogamp.jogl:jogl-all:$config.jogAmpVer:natives-windows-i586"
}